"""Layer 3 — macro / regime + sector-rotation layer (Phase 3).

A risk-on vs risk-off classifier built from macro inputs (dollar, 2y/10y yields,
oil, VIX, breadth) starting as simple scoring rules; sector rotation falls out of
relative-strength comparisons across sector ETFs. Feeds the decision layer.
"""
