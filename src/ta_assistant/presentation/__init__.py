"""Layer 5 — backtesting & presentation layer (Phase 4-5).

Out-of-sample validation (walk-forward, net of costs incl. borrow) plus annotated
charts (mplfinance — the `viz` dependency group) and diff-driven alerts. Rendering
happens inside activities.
"""
